<section>
  <div class="content-wrapper-narrow content-container">
    <h3><%= t(".header") %></h3>

    <%= render("delivery_zones/defaults", delivery_zone: @scheduled_pickup.delivery_zone) %>

    <p><%= t(".prompt") %></p>

    <%= render("scheduled_pickups/form", scheduled_pickup: @scheduled_pickup) %>
  </div>
</section>
